#include <SDL.h>
#include <SDL_image.h>

#include <iostream>

class Texture {
   protected:
    SDL_Texture* texture;
    std::string path;

   public:
    void setPath(std::string path) {
        }
}
